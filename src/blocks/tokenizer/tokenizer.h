#ifndef TOKENIZER_H
#define TOKENIZER_H

// Max tokens and token length constants
#define MAX_TOKEN_LENGTH 100
#define MAX_TOKENS 100

// Function declaration for tokenization
int tokenize(const char *text, int *token_ids);

#endif
